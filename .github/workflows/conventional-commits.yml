# Modified by BW-Tech GmbH: automatic conventional-commit validation is disabled for BW-Tech forks.
name: Conventional Commits

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Conventional commit validation is disabled for BW-Tech forks."